{
  "buildCommand": "python3 manage.py migrate && mkdir -p public/static && cp -r static/* public/static/ 2>/dev/null || true",
  "env": {
    "DEBUG": "False",
    "ALLOWED_HOSTS": "sai-shritha-pamula-courture-portfol.vercel.app,.vercel.app"
  }
}
